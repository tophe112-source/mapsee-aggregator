@AGENTS.md

<!-- Nothing else here, on purpose. AGENTS.md is the one map every model reads
     (Claude imports it above; Codex and Cursor read it directly), and the
     measured notes live in docs/agents/ — grep docs/agents/INDEX.md for the
     symptom, open one file. When you learn something worth keeping, add it to
     the topic file with its measurement and run `python agent_notes.py --index`. -->
