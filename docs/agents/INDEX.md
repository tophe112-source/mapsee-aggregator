# Index of mapsee-aggregator's agent notes

Generated by `python agent_notes.py --index`; do not hand-edit. One line per note, grouped by file — grep this, then open the file.

## The adapters and what individual sources taught — `docs/agents/adapters-and-sources.md` (13)

- Luma's Discover feed takes `discover_place_api_id`.
- One adapter ingests BUSINESSES, not events, and that is a different promise.
- parkrun start times are NOT in the feed, and they must not be guessed.
- One malformed record cost an entire site, and the log said "FAILED".
- Reading a source correctly BY ACCIDENT is not reading it.
- A venue calendar carries entries that are not events, and they are the best-formed rows in the feed.
- A metro Overpass never answered for is UNREAD, and the cursor must not move past it.
- `webcal://` is `https://` wearing a hat, and `requests` has never heard of it.
- A regex that FINDS a JSON-LD block is not a parser that can READ it, and discovery must use the parser.
- VERIFYING IS NOT INGESTING, AND A CITY PUBLISHES ITS SHUT DAYS.
- A HELPER IMPORTED IS A CONTRACT INHERITED, AND BOTH OF THIS ADAPTER'S PRODUCTION FAILURES WERE ONE GUESSED RATHER THAN READ.
- A CONFIG THAT PARSES AS JSON IS NOT A CONFIG THAT LOADS.
- The obvious copy of a fact is the wrong one, and seattlecenter.com offers three at once.

## Brazil: Mapas Culturais and what an empty country meant — `docs/agents/brazil-mapasculturais.md` (7)

- A FILTER CAN BE ACCEPTED AND NEVER RUN, and the count it returns is then the WHOLE ARCHIVE.
- AND THE DATE IS IN THE BLOB, NOT THE COLUMN.
- `{"latitude": "0", "longitude": "0"}` IS THE COMMONEST COORDINATE IN THAT SOURCE AND IT IS A STRING, so it passes every presence check.
- A PLATFORM CAN INTERPOLATE ITS OWN MISSING-VALUE PLACEHOLDER INTO AN ADDRESS.
- `timezoneName` SAID `Etc/UTC` ON ALL 1,938 OCCURRENCES AND IT IS NOT TRUE.
- BRAZIL WAS EMPTY BECAUSE NOTHING EVER ASKED.
- PARKRUN AND MOBILIZON ARE MEASURED NEGATIVES IN BRAZIL, written down so nobody re-derives them.

## CI: the workflows, budgets, timeouts, job order and what a guarded job needs — `docs/agents/ci-and-jobs.md` (14)

- `--only-new` is the default in CI.
- Every ingest job is deliberately failure-tolerant
- A CONFIG FILE A GUARDED JOB NEEDS IS PART OF THE JOB.
- A FAILED READ IS NOT A FAILED RUN, and `sys.exit(1)` cost seven domains their daily push.
- A STEP CANCELLED BY `timeout-minutes` SKIPS EVERY STEP AFTER IT, INCLUDING THE ONE THAT SAVES THE WORK.
- AND THE BUDGET GOES WHERE THE TIME GOES, WHICH IS NOT WHERE THE LOOP IS. Twice, in the same fix.
- `always()` IS THE HALF THAT ACTUALLY SAVES THE WORK, and it is worth having even when the budget is right.
- A JOB THAT WORKS FOR AN HOUR AND PUSHES ONCE IS A JOB THAT LOSES A RACE IT NEVER KNEW IT WAS IN.
- A minus sign is an option, not a number.
- Never add `pull_request:` to a workflow that reads secrets.
- THE ORDER OF THE THREE DAILY JOBS IS LOAD-BEARING.
- A MIGRATION'S CODE MERGES BEFORE THE MIGRATION RUNS, and nothing sequences the two.
- A STEP ADDED TO A LONG JOB IS ADDED TO ITS CRITICAL PATH, and `feeds` was already at 3h29m against a 240-minute cap.
- A HELPER THAT WRITES A REPO FILE AS A SIDE EFFECT WILL EVENTUALLY BE CALLED BY A TEST.

## Classification: which front door an event reaches — `docs/agents/classification-and-categories.md` (11)

- A category key that no lens opens onto reaches only mapsee.me.
- "Order pickup" must be earned by the URL, never by the category.
- A classifier fix reaches the FUTURE only.
- A lens can be starved by its CLASSIFIER rather than by its sources.
- A venue's event calendar is often not the thing the venue IS.
- A fuzzy search's keyword is not a classification, and `market` is the word that proves it.
- A config's `category` is a DEFAULT, so the right value depends on whether the calendar is pure or mixed.
- The `kids` layer is fed by a REGEX, not by sources, and it was missing a third of its supply.
- Widening a kids rule catches the adults' version of the same programme.
- `brunch` was the commonest food word on the map and the food rule did not have it.
- A specific-but-wrong category DEFAULT is worse than a vague right one.

## Finding sources: catalogs, discovery, verification, refusals and licences — `docs/agents/curation-and-discovery.md` (24)

- The curation ledger records candidates too.
- `status` has THREE values, and `empty` is not `fail`.
- The audit retries; candidate verification does not.
- `_not_included` in a sources file is an editorial NO, and discovery reads it.
- A widget key on somebody's page is not a feed you may read.
- The stock Squarespace robots.txt disallows `?format=json`, on every site on the platform.
- A failed fetch is not a dead page, and conflating them is expensive.
- National CKAN portals publish SPREADSHEETS ABOUT events, not event feeds.
- A CURSOR MUST ADVANCE BY WHAT WAS EXAMINED, and "the window's length" stops being that the moment anything can stop early.
- A curated city list is only as complete as its last audit, and Seattle has TWO market operators.
- A search endpoint that ignores every paging parameter still answers 200.
- The catalogs cannot see the long tail, and the map can.
- Detecting a site BUILDER is not detecting a calendar.
- A bot challenge does not answer 403, and the 200 is the dangerous one.
- "We cannot read it" is two findings, and only a SECOND NETWORK tells them apart.
- A feed can pass verification and still put nothing on the map.
- A refusal is not a fact about the site, so it must not be written down as one.
- Detecting a calendar plugin tells you it HAS events, not what it will hand you.
- A deep event page can be a better crawl seed than the index, and only measurement says which.
- A platform can imply a feed URL the page never links.
- `cmd_merge` rewrote whole files to add one line.
- A calendar PAGE is often not the whole calendar; the sitemap is.
- Cloudflare's bot challenge is a NO, and curl getting through is not a second opinion.
- A whole national catalogue can sit on one licence, and it is worth checking.

## Dates, offsets and timezones — `docs/agents/dates-and-timezones.md` (9)

- When a listing carries two dates, the one the template shows a human is the wrong one.
- A recurring event with no end date projects pins forever, and nothing downstream will ever remove them.
- A date-dependent assertion is not flaky, it is wrong one day in seven.
- A timestamp's offset can belong to the SERVER, not the event.
- A single-venue calendar's `startDate` may carry no offset at all, and that is the venue block's real job.
- Counting records is not checking dates.
- A source can hand you a SENTINEL date, and `mapsee_cleanup.py` can never remove it.
- A BARE DATE NAMES A DAY *SOMEWHERE*, AND FOR MONTHS THAT SOMEWHERE WAS GREENWICH.
- `starts_at`'s FIRST TEN CHARACTERS ARE NOT RELIABLY THE LOCAL DATE.

## Geocoding, addresses and coordinates — `docs/agents/geocoding-and-addresses.md` (6)

- A source that HANDS you coordinates can hand you the wrong ones, and nothing downstream will catch it.
- A comma-counting address parser assumes a street.
- The only geocoder is US Census, so OUTSIDE the US a source must bring its own coordinates.
- The city is not a street, and putting it in `address` invites the geocoder to move the pin.
- A GEOCODER THAT ANSWERS EVERY QUESTION IS NOT ONE THAT KNOWS EVERY ANSWER, and the defence is the CONFIG rather than the geocoder.
- A LICENCE THAT SAYS YES IS THE EASY HALF, AND THE GEOCODER IS THE HARD ONE.

## Source health, baselines and quiet sources — `docs/agents/health-and-monitoring.md` (4)

- The health check reads `stats_snapshot_all()`, NOT `source_stats()`.
- A source in the health report is `external_source`, which is `'mapsee'` for everything this repo writes.
- A green source-health run with no baseline means "no opinion", not "healthy".
- A source that runs one day a week has no retry, and nothing notices.

## BiblioCommons library programmes — `docs/agents/libraries-bibliocommons.md` (4)

- THE EXPENSIVE HALF OF A CIVIC FEED IS THE GEOCODING, AND BIBLIOCOMMONS HANDS IT OVER.
- ITS DATE FILTER IS ACCEPTED AND IGNORED.
- THE SHARED FINGERPRINT IS DATE-GRANULAR, AND A LIBRARY RUNS THE SAME PROGRAMME TWICE IN A DAY.
- MOST OF A LIBRARY'S EVENT IMAGES ARE ONE STOCK TILE.

## OpenActive/RPDE: booking grids, weekly collapse, standing rows, retirements — `docs/agents/openactive-and-standing-rows.md` (15)

- AN RPDE FEED PAGES FROM THE OLDEST END, so reading page one tells you nothing — and it lies in BOTH directions.
- A `ScheduledSession` does not know its own name, and 93 of 127 publishers are not publishing events at all.
- A SESSION PUBLISHED EVERY TEN MINUTES IS A BOOKING GRID, and it walks past the refusal written for exactly that.
- AND THE HORIZON WAS THE OBVIOUS LEVER AND THE WRONG ONE.
- A PERFORMANCE MEASUREMENT ON THIS DATABASE IS A MEASUREMENT OF THE CACHE, and it will invert under you mid-session.
- THE OCCURRENCE FEED IS USELESS WITHOUT THE SERIES FEED, so one transient 500 cost 63,141 sessions.
- AND THE PAGE CAP IS A CEILING, NOT A PLAN — the same lesson as the job timeout, one file over.
- EVERY SLOT IN THAT GRID WAS PUBLISHED TWICE, and only counting distinct start times showed it.
- AN UPSERT CANNOT DELETE, so the run that landed the collapse made the map WORSE before it made it better.
- THE GRID WAS THE VISIBLE TENTH, AND THE OTHER 88% WAS ONE PUBLISHER PUBLISHING HONESTLY.
- AND "THREE POINTS MAKE A PATTERN" FOLDED NOTHING.
- THE LICENCE LINE IS THE LAST LINE, AND THE SYNC CUT FROM THE END.
- A STANDING ROW NEVER DIES, AND RUN #55 CREATED FORTY THOUSAND OF THEM.
- A SECOND COLLAPSE NEEDS A SECOND RETIREMENT RULE, and the first one cannot see the second's orphans.
- AND THE ESCAPE HATCH HAD NEVER WORKED.

## OSM amenities: which civic places earn a pin, a sheet, or nothing — `docs/agents/osm-amenities.md` (23)

- ONE SELECTOR IS NOT ALWAYS A CATEGORY.
- MOST CIVIC PLACES HAVE NOTHING WORTH READING, and pretending otherwise is how you ruin a map.
- A DENY-LIST WHOSE COMMENT DESCRIBES AN ALLOW-LIST WILL BE WRONG FOR EVERY ADAPTER ADDED AFTER IT.
- A BARE ONE OF THESE IS NOT ALWAYS WORTH A DOT, and `tourism=artwork` is the selector that proves it.
- AND `--only-new` WAS NEVER RIGHT FOR THIS ADAPTER, only unexamined.
- A CACHED ELEMENT LIST CANNOT SEE A NEW SELECTOR, and the workflow comment that says so is not a mechanism.
- `amenity=toilets` is 519,045 uses and was the obvious omission.
- AND AN UPSERT CANNOT DELETE, so refusing them at ingest left 120 already on the map.
- AND THE THIRD FAILURE WAS ON THE LINE THAT PRINTS THE RESULT.
- A FACT BUYS A SHEET, NOT A LISTING — "can it be SHUT" is the Nearby test.
- AN OPERATOR THAT RESTATES THE THING IS NOT A FACT — "a name is not a fact", one tag over.
- OSM WRAPS SOME DESCRIPTIONS IN QUOTES, AND THE HOVER LABEL IS WHERE IT SHOWS.
- A VALUE THAT MATCHES THE ASSUMPTION IS NOT A FACT — "a name is not a fact", one level down, and missed on the first pass.
- `social_facility=food_bank` is 4,938 uses; `amenity=food_bank` is 16.
- "No hours tagged" means opposite things for a playground and for a food bank.
- A FOOD BANK IS THE ONE PIN WHERE "YOU CANNOT CLICK IT" IS THE WRONG ANSWER, and getting there took making it furniture first.
- A ROW NOBODY COULD OPEN WAS A ROW NOBODY HAD READ.
- A HAND-WRITTEN FIXTURE CANNOT SEE A BUG THAT LIVES BETWEEN TWO FILES, so `gen_amenity_fixtures.py` generates them.
- A CATEGORY IS NOT A KIND, AND THE MAP DREW THREE THINGS AS ONE DOT.
- AND A GLYPH FIX REACHES THE FUTURE ONLY.
- A REWRITE-EVERY-RUN ADAPTER IS RIGHT AND "REWRITE EVERYTHING" IS NOT, and the second half was found on the READ side of another repo.
- AND THE WINDOW ON A STANDING ROW IS NOT OURS, WHICH WOULD HAVE MADE THAT FILTER A NO-OP ON EXACTLY THE ROWS IT WAS WRITTEN FOR.
- A COLUMN FED BY THE CLOCK WOULD UNDO IT ALL WITH ONE LINE IN ANOTHER FILE, AND THE LOG WOULD READ AS A BUSY WEEK.

## Platforms probed, and what they cost — `docs/agents/platforms-probed.md` (2)

- A 403 IS A REFUSAL AND NOT AN OBSTACLE.
- `max(start_date)` IS NOT A LIVENESS TEST.

## Spam, advertisements and the content gate — `docs/agents/spam-and-content.md` (6)

- A FEED THAT WORKS PERFECTLY CAN STILL BE FULL OF ADVERTISEMENTS, and verification will never say so.
- A HIGH SPAM RATE IS NOT ENOUGH TO RETIRE A SOURCE; ZERO UNIQUE SUPPLY IS.
- THE HALF OF A CONTENT FILTER THAT FAILS SILENTLY IS THE HALF THAT REFUSES SOMETHING REAL.
- AN IMPLAUSIBLE END DATE IS A FACT WE CANNOT USE, NOT A VERDICT ON THE ROW — and the audit is what proved it.
- A GATE FIXES THE FUTURE ONLY
- WHAT MAKES A CONTENT-BASED DELETE SAFE TO SCHEDULE IS NOT THE SCOPE FILTER, IT IS THE SHARE CEILING.

## US state fairs as events — `docs/agents/state-fairs.md` (5)

- A STATE FAIR'S WEBSITE IS MARKETING COPY, SO ITS DATES ARE THE HEADLINE AND SO IS EVERYTHING THAT LOOKS LIKE THEM.
- THE DATES ARE NEVER WRITTEN DOWN, AND THAT IS THE POINT.
- A YEAR CAN SIT ON THE WRONG SIDE OF ITS DATES.
- A TOWN IS NOT A VENUE, AND THE DIFFERENCE IS A `venue` BLOCK.
- PHOTON CANNOT READ "VENUE - STREET CITY ST ZIP", WHICH IS HOW EVERY CIVICPLUS SITE WRITES EVERY LOCATION.

## The sync, EventStore, upserts, paging, cursors, fingerprints and series — `docs/agents/sync-eventstore-and-paging.md` (11)

- An upsert cannot delete, so a fix to what we WRITE never reaches what is already written.
- OFFSET paging against our own database gets dearer every page, and it fails the day the catalog outgrows it.
- A KEYSET FIXED THE COST PER PAGE AND LEFT THE NUMBER OF PAGES UNBOUNDED, which is the half that broke next.
- "A SUBSET OF THE SITEMAP" IS AN INVARIANT TWO FILES HAVE TO KEEP, AND ONE OF THEM MOVED.
- `raise_for_status()` throws the diagnosis away.
- Two different 5xx come back from PostgREST and they want opposite things.
- One source id can mean many events, and EventStore deletes on the collision.
- `--ignore-cursor` MAKES A RUN UN-REPEATABLE, so fusing it with "rewrite existing rows" made a backfill impossible to finish.
- `make_fingerprint` is date-keyed on purpose, and a matinee is not the evening show.
- `series_id` is assigned after the fact, not at ingest.
- A TOGGLE'S MEANING CAN BE OVERTAKEN BY A NEW KIND OF ROW.
